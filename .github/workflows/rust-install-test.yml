name: "Rust crate install test"

on:
  # Just to test it in the first place, will be replaced later
  workflow_dispatch:
        
  # push:
  #   paths:
  #     - "quaynor/rust/**"
  #     - "quaynor/core/**"
  #     - ".github/workflows/rust-install-test.yml"
  # pull_request:
  #   paths:
  #     - "quaynor/rust/**"
  #     - "quaynor/core/**"
  #     - ".github/workflows/rust-install-test.yml"

jobs:
  ubuntu:
    name: "Ubuntu (apt)"
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake clang libclang-dev build-essential \
            libvulkan-dev libshaderc-dev glslc vulkan-tools

      - name: cargo check
        working-directory: quaynor
        run: cargo check -p quaynor-rust

  macos:
    name: "macOS"
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          echo "$HOME/.cargo/bin" >> $GITHUB_PATH

      - name: Install libomp
        run: brew install libomp

      - name: cargo check
        working-directory: quaynor
        run: |
          export CC=/usr/bin/clang
          export CXX=/usr/bin/clang++
          export LDFLAGS="-L$(brew --prefix libomp)/lib"
          export CPPFLAGS="-I$(brew --prefix libomp)/include"
          cargo check -p quaynor-rust

  windows:
    name: "Windows"
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        run: |
          curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
        shell: bash

      - name: Install LLVM
        uses: KyleMayes/install-llvm-action@v2
        with:
          version: "17"

      - name: Install Vulkan SDK
        run: choco install vulkan-sdk -y

      - name: cargo check
        working-directory: quaynor
        env:
          LIBCLANG_PATH: ${{ env.LLVM_PATH }}/lib
        run: cargo check -p quaynor-rust

  nix:
    name: "Nix"
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - uses: cachix/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable

      - name: cargo check inside nix shell
        run: |
          nix-shell quaynor/shell.nix --run "cd quaynor && cargo check -p quaynor-rust"
